server {
    listen 80;
    server_name clinic.mrsinghdev.com;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl http2;
    server_name clinic.mrsinghdev.com;

    ssl_certificate /etc/letsencrypt/live/clinic.mrsinghdev.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/clinic.mrsinghdev.com/privkey.pem;

    root /var/www/meraclinic/frontend/dist;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }
}
